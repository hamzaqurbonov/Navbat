<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bc.sartarosh">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sartarosh"
        tools:targetApi="31">
        <activity
            android:name=".profil.EditProfileActivity"
            android:exported="false" />
        <activity
            android:name=".customer.CustomerBarberActivity"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.gms.client_id"
            android:value="@string/default_web_client_id" />

        <activity
            android:name=".profil.BarberActivity"
            android:exported="false" />
        <activity
            android:name=".customer.CustomerActivity"
            android:exported="false" />
        <activity
            android:name=".profil.LoginActivity"
            android:exported="false">

        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>